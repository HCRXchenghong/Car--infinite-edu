# 当前状态

**文档审查基线：** `2026-09-19`  
**范围：** 教育版四轮四转底盘的 VCU、BCM 架构和 PCB 打板准备文档。  
**已增加：** [HW-R1 硬件实施包](硬件设计包/00_使用说明与放行边界.md)、打印 PDF、可编辑 DOCX、228 脚建议 Pinmap、35P 逻辑分配、候选 BOM、12 路 UART 映射、26 项检验程序、空白记录及 16 份受哈希约束的厂家/商户资料与官方引脚数据库；其中 DM-J10010L-2EC 含手册、2D、3D 和性能曲线。

**不包含：** 可烧录固件、真实 CAD 原理图、PCB/Gerber、完整可采购生产 BOM、已冻结物理腔号的制造线束图、实车标定或实际硬件测试合格记录。

**打印交付：** 主手册 61 页 + 完整横向作业册 43 页，共 104 页；含 228 个 MCU 实体脚、15 颗其他核心芯片的 310 条焊盘/EP 记录、142 条参考外围逐端连接与 24 条 MCU 对端复核，并加入 DM-J10010L 四轴身份配置、限制参数和标定记录。PDF 内容校验和有限版面检查已完成，见 [打印说明](硬件设计包/打印版/打印与交付说明.md) 和 [版面抽检记录](硬件设计包/打印版/版面抽检记录.md)；这些检查不等同于硬件通过验证。

**工程就绪度：** 未达到原理图冻结或 PCB 投板条件。完整阻断项和放行门槛见 [项目完整审查与工程放行报告](PROJECT_AUDIT.md)。

## 已明确的设计边界

- VCU 是底盘运动的唯一控制中心，负责四轮四转解算、执行器控制和硬件急停链路；BCM 不绕过 VCU 控制底盘运动。
- VCU 首版以 `12V / 3A` 连续、`5A` 短时峰值为设计目标，PCB 板框目标为 `130 mm x 90 mm`。四个驱动轮和四个转向执行器的动力电源不经过 VCU PCB；八个执行轴都必须纳入硬件安全链。
- BCM 首版以 `12V / 15A` 连续、`20A` 短时峰值为设计目标，K1 至 K8 的继电器触点输出每路目标不超过 `2A`，PCB 板框目标为 `140 mm x 100 mm`。这些数值必须由实际负载矩阵、压降和温升测试闭合。
- BCM 的正式对外线束统一通过单一多针防水汽车母座 `J_BCM_EXT` 接出；首版候选为 `K776280WV-35-PTSNB`（C2997460）。修订后的 35P 逻辑分配包含电磁锁两根功率线，不再引出未定义用途的 5V/3V3；厂家图纸和样件仍未确认。
- 主干通信为 CAN FD，VCU、BCM 和上位机的具体 CAN ID、信号、周期、超时、CRC 与 Alive Counter 尚未冻结。
- CAN2 转向电机已识别为四台 `DM-J10010L-2EC`：经典 CAN 标准帧、默认 1Mbps、四种控制模式。装车前必须逐台设置唯一 `ESC_ID/MST_ID` 并读回限制参数；完整使能/失能协议、反馈 D0 位定义、TIMEOUT 单位/默认值、硬件安全输入和失电保持仍未关闭。
- CAN3 驱动器已识别为两台 `MSSD-100EMA_2EC_N`。协议是厂家自定义 CAN 2.0A 标准帧，命令结构类似 CANopen SDO 但资料明确不支持 CANopen；默认两台都是 500kbps/节点 1，装车前必须改成唯一节点。主动上报定义和完整寄存器表仍缺失。
- 锁候选为 `SM60E DC12V`，资料值 92mA/1.1W、通电生磁、断电消磁；双白“无磁反馈”不能未经验证视为门体机械到位。推杆仍只有商户口述的两线换极与内置双限位，H07 未关闭。

## 未完成与发布限制

- VCU 的轴距、轮距、轮胎有效半径、执行器方向和转向零位必须以实车测量和标定结果为准，当前不写死具体数值。
- BCM/VCU 的连接器物理针号、端子压接规格、线束线径、安装孔、连接器朝向和外壳干涉尚未最终确认。
- VCU/BCM 的 MCU Pinmap、控制权仲裁、八轴急停路径和 CAN/UART 物理层尚未冻结；DM-J10010L 与 MSSD 已有部分供应商协议，但各自缺项仍阻止完整通信/安全冻结。
- BCM 的 K1 实际电流、完整功率预算和 2A 支路独立保护尚未闭合。
- 投板前必须完成原理图与 PCB 设计、ERC/DRC、BOM/坐标/Gerber 一致性、端子温升和压降、3D 机械干涉，以及实际负载启动峰值和最坏同时开启组合验证。
- 本版本仅发布设计与制造准备文档，不能作为生产放行或实车安全验收的依据。

## 本次变更

- 补充可打印硬件实施包，明确证据等级、逐脚连接、回流、安全默认态、计算与检验；数据脚本验证全部 228 个 MCU 焊盘覆盖和所选 GPIO 功能存在、EXTI 无冲突。
- 采用 TPSM53603 和带 VIO 的 TCAN1044AV-Q1 作为参考单元；MAX14830 补齐 VEXT；MC33926 补齐 CCP、SF 上拉和输入拉流校核。
- 二轮复核确认 MC33926PNB 为 8×8mm 的 32 引脚 PQFN，纠正中间稿封装误写；Buck PGOOD 默认不接 MCU，避免未校核电源顺序导致高于 VIN+0.3V。
- 新增 15 颗核心芯片 310 条焊盘/EP 连接、142 条参考外围逐端连接和 24 条 MCU 点对点核对记录；未知模块/安全器件/连接器仍不冒充完整 CAD 网表。
- BCM 15A 预算仍未闭合；G474 数据库 43/44 脚定义有歧义，必须取得完整数据手册后核验。外部真实型号、连接器图纸和安全电路选型见硬件包 H01～H16。
- 完成全仓库架构、接口、电源、安全、制造与验证审查，并新增工程放行报告。
- 修正 BCM 35P 已分满却遗漏电磁锁功率线的问题；删除未定义用途的外部 5V/3V3，引入专用 CAN/UART 参考地。
- 将 BCM 支路保护、K1 实测负载和功率预算闭合列为投板阻断项。
- 将 VCU 八轴硬件安全、无线控制模式仲裁、MCU Pinmap 和 UART 物理层列为原理图/投板阻断项。
- 依据供应商协议纠正 CAN3 的 CANopen 假设：MSSD 仅使用 SDO-like 的自定义经典 CAN；新增 SM60E 和推杆证据边界，避免把“无磁反馈”或商户口述限位当作已验证安全功能。
- 纳入 DM-J10010L-2EC 转向电机 V1.1 手册、2D/3D 和性能曲线；将 CAN2 冻结为经典 CAN 1Mbps 的厂家协议输入，并新增四轴 ID、限制参数、零位/方向、终端和协议冲突记录表。
