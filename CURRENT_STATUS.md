# 当前状态

**发布基线：** `v0.1.0`  
**范围：** 教育版四轮四转底盘的 VCU、BCM 架构和 PCB 打板准备文档。  
**不包含：** 可烧录固件、原理图、PCB 源文件/Gerber、BOM、线束图、实车标定数据或硬件测试记录。

## 已明确的设计边界

- VCU 是底盘运动的唯一控制中心，负责四轮四转解算、执行器控制和硬件急停链路；BCM 不绕过 VCU 控制底盘运动。
- VCU 首版按 `12V / 3A` 连续、`5A` 短时峰值预留，PCB 板框目标为 `130 mm x 90 mm`。四个驱动轮和四个转向执行器的动力电源不经过 VCU PCB。
- BCM 首版按 `12V / 15A` 连续、`20A` 短时峰值校核，K1 至 K8 的继电器触点输出每路不超过 `2A`，PCB 板框目标为 `140 mm x 100 mm`。
- BCM 的正式对外线束统一通过单一多针防水汽车母座 `J_BCM_EXT` 接出；首版候选为 `K776280WV-35-PTSNB`（C2997460），但尚未完成厂家图纸和样件确认。
- 主干通信为 CAN FD，VCU、BCM 和上位机的具体 CAN ID、信号、周期、超时、CRC 与 Alive Counter 尚未冻结。

## 未完成与发布限制

- VCU 的轴距、轮距、轮胎有效半径、执行器方向和转向零位必须以实车测量和标定结果为准，当前不写死具体数值。
- BCM/VCU 的连接器物理针号、端子压接规格、线束线径、安装孔、连接器朝向和外壳干涉尚未最终确认。
- 投板前必须完成原理图与 PCB 设计、ERC/DRC、BOM/坐标/Gerber 一致性、端子温升和压降、3D 机械干涉，以及实际负载启动峰值和最坏同时开启组合验证。
- 本版本仅发布设计与制造准备文档，不能作为生产放行或实车安全验收的依据。

## 本次变更

- 统一 VCU/BCM 的板框、电源预算、接口边界和待验证项。
- BCM 文档更新为 V1.2，收敛为单一 `J_BCM_EXT` 多针车载母座方案，并明确 15A/20A 总输入和每路 2A 继电器触点约束。
- VCU 文档补充 130 mm x 90 mm 板框、3A/5A 电源预算、小信号连接器方向和实车几何参数标定要求。
